
[build-system]
requires = ["setuptools==73.0.1", "wheel", "numpy==1.23.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mapper_c_utils"
version = "1.2.0"
description = "C utilities for mapper"
requires-python = ">=3.8"
dependencies = ["numpy==1.23.0"]
