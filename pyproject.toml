
[build-system]
requires = ["setuptools>=67.2", "wheel", "numpy==1.26.4"]
build-backend = "setuptools.build_meta"

[project]
name = "mapper_c_utils"
version = "1.1.0"
description = "C utilities for mapper"
requires-python = ">=3.12"
dependencies = ["numpy>=1.26.4"]
