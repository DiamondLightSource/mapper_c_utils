
[build-system]
requires = ["setuptools>=61.0", "wheel", "numpy<2.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mapper_c_utils"
version = "1.1.0"
description = "C utilities for mapper"
requires-python = ">=3.8"
dependencies = ["numpy"]
